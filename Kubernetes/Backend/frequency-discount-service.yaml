apiVersion: v1
kind: Service
metadata:
  name: frequency-discount-service
  namespace: backend
spec:
  selector:
    app: frequency-discount
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
